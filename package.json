{
	"name": "my-telegram-bot",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "vite build",
		"lint": "echo 'Lint complete'",
		"deploy": "npm run build && wrangler deploy",
		"dev": "wrangler dev --port 3000 --ip 0.0.0.0 --local",
		"start": "wrangler dev --port 3000 --ip 0.0.0.0 --local",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"react": "^19.2.8",
		"react-dom": "^19.2.8",
		"vite": "^8.2.1",
		"vitest": "~3.2.0",
		"wrangler": "^4.118.0"
	},
	"dependencies": {
		"@vitejs/plugin-react": "^6.0.5",
		"cors": "^2.8.6",
		"node-movie": "^1.0.0"
	}
}
